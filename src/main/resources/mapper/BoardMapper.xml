<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.choongang.concert.repository.BoardMapper">

	<select id="noticeBoardFindAll" resultType="Notice">
		select id, category, title, crated_at , view_cnt from 
		notice_board order by id desc limit 1,15;
	</select>
	
	
	
	<select id="qnaBoardFindAll" resultType="QnaList">
		select category, title, crated_at, user_id, response_yn
		from qna_board order by crated_at desc limit 1,15;
	</select>
</mapper>

